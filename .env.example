# Public origin(s) of the reverse proxy, comma-separated.
ALLOWED_ORIGINS=https://belote.example.invalid
# Keep these defaults for the Compose deployment.
WS_HEARTBEAT_INTERVAL_MS=15000
WS_HEARTBEAT_TIMEOUT_MS=30000
# Never enable this outside isolated E2E environments.
E2E_FIXTURE=0
